name: Create PR after push

on:
  push:
    branches:
      - main

jobs:
  create-pr:
    if: github.actor != 'readme-generate'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Set Branch Name
        run: echo "BRANCH_NAME=review/auto-pr-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_ENV

      - name: Make dummy commit
        run: |
          echo "Trigger PR at $(date)" > .pr-trigger
          git add .pr-trigger
          git commit -m "chore: trigger PR automático"

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.PERSONAL_GITHUB_TOKEN }}
          commit-message: "chore: PR automático para análise"
          branch: ${{ env.BRANCH_NAME }}
          title: "🔍 PR Automático - ${{ env.BRANCH_NAME }}"
          body: |
            Este PR foi criado automaticamente após push na main.
            Branch gerada: `${{ env.BRANCH_NAME }}`.

            ⚠️ Este PR contém um commit dummy (.pr-trigger) apenas para viabilizar a criação. Pode ser ignorado.
          delete-branch: true
